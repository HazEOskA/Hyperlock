[project]
name = "project-memory-controller"
version = "0.1.0"
description = "Deterministic project state layer for AI-assisted development workflows."
requires-python = ">=3.11"
dependencies = [
  "pyyaml>=6.0.0"
]

[project.scripts]
pmc = "pmc.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
